export class Items{
    id!: number;
    product!: string;
    name!: string;
    quantity!: number;
    price!: number;
    // total!: number;

    // constructor() {
    //     this.quantity = 1; // Default quantity
    //     this.price = 0; // Default price
    //     this.total = 0; // Default total
    // }
}