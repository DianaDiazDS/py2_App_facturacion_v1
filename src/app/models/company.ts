import { Address } from "./address";

export class Company{
    fiscalNumber!: number;
    name!: string;
    // address!: Address;
    phone!: string;
    email!: string;
    logo!: string;

    // constructor() {
    //     this.address = new Address();
    // }
}