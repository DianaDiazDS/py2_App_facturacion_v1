import { Address } from "./address";

export class Company{
    fiscalNumber!: number;
    name!: string;
    // address!: Address;
    phone!: number;
    email!: string;

    // constructor() {
    //     this.address = new Address();
    // }
}